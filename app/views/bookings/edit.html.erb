<%= simple_form_for [@city, @booking] do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.submit %>
<% end %>

<% @reviews.each do |review| %>
  <% if policy(review).edit? %>
    <%= link_to "Update", edit_city_review_path(review) %>
  <% end %>
<% end %>
